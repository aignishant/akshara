"""Project Akshara — a language model built from the byte up.

Every line under this package is typed from a day document. Nothing is pre-written.
Subpackages are pipeline stages, in the order the curriculum builds them:
tokenizer -> model -> train -> infer -> eval -> serve.
"""
