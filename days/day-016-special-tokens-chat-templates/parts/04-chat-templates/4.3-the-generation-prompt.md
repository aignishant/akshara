---
day: 16
part: "4.3"
title: "The generation prompt — the turn you open and do not close"
ids: ["TOK-17"]
level: working
prerequisites: ["4.2-roles-as-markers.md"]
prev: "4.2-roles-as-markers.md"
next: "4.4-the-same-template-as-a-template.md"
---

# 4.3 — The generation prompt — the turn you open and do not close

## One-line answer

To make the model answer you append an **opened, empty assistant turn** — `<|im_start|>assistant\n` — which
costs **5 tokens** here, taking the conversation from 55 ids to 60, and four of those five are the word
`assistant` spelled out in ordinary pieces (`ass`, `ist`, `ant`) followed by a newline.

---

## The story

At a small clinic, patients are called by a receptionist who writes each name on a slip and puts it in a
tray for the doctor.

When the doctor is ready for the next patient, the receptionist does not say anything. They put a **blank
slip** on the top of the tray, with the room number written on it and nothing else.

The blank slip means *your turn now*. It is not a message. Nobody wrote anything on it. Its entire meaning
is that it is there, in the right place, unfinished — and everyone in the room has learned what an
unfinished slip on top of the tray means, because they have seen it happen forty times a morning.

---

## The idea in plain language

Part [4.2](4.2-roles-as-markers.md) rendered a conversation that was already complete — three turns, each
one opened and closed. That string is what you would use as a **training example**: the model learns to
predict every token in it, including the assistant's answer.

At inference you have a different problem. The conversation ends with the user's turn, and you want the
model to produce the assistant's. So you write the *beginning* of the assistant's turn and stop:

```
<|im_start|>user
What is a merge list?<|im_end|>
<|im_start|>assistant
```

No content, no closing `<|im_end|>`. The string ends mid-turn.

**That is the generation prompt**, and its logic is simply that a language model continues text. The model
has seen, in training, tens of thousands of examples where `<|im_start|>assistant\n` is followed by an
assistant's reply and eventually by `<|im_end|>`. Presented with that prefix and nothing after it, the most
probable continuation is a reply — and then a stop marker, which is what the serving loop watches for.

It is controlled by a boolean argument, conventionally called `add_generation_prompt`, and the two states
correspond to the two jobs:

| `add_generation_prompt` | What you get | What it is for |
| --- | --- | --- |
| `False` | every turn opened and closed | building **training** examples |
| `True` | plus an opened, empty assistant turn | **inference** |

**Getting this backwards is a specific, common bug in each direction.** Train on strings that carry a
generation prompt and every training example ends with a dangling opened turn the model must learn to
continue with nothing. Serve without one and the model is asked to continue a *completed* conversation,
whose most probable continuation is a new `<|im_start|>` — so it invents the user's next question and
answers that too.

---

## Why Akshara needs it

Day 88 builds the training set and Day 150 builds the request handler, and they call the same template with
the flag set differently. That is the whole reason the flag exists as an argument rather than as two
functions: two functions drift, one function with a flag cannot.

More sharply: the **last token before generation** is the one that conditions the model's very first output
token. Part [5.1](../05-template-mismatch/5.1-one-character-three-tokens-a-different-model.md) measures a
template whose only difference from this one is that final token — a space instead of a newline — and shows
that the two prompts have the same length and differ in exactly one id, at the position where it matters
most. Day 17's TOK-19, token healing and the trailing-space bug, is the general form of that problem.

---

## The mechanism

```python
# lab/chat_03.py  -  T0, laptop CPU. Continues lab/chat_02.py.
def render_by_hand(messages: list[dict[str, str]], add_generation_prompt: bool = False) -> str:
    """Flatten a conversation into ChatML; optionally open an empty assistant turn."""
    out = []
    for m in messages:
        out.append("<|im_start|>" + m["role"] + "\n" + m["content"] + "<|im_end|>" + "\n")
    if add_generation_prompt:
        out.append("<|im_start|>assistant" + "\n")
    return "".join(out)


for agp in (False, True):
    s = render_by_hand(chat, add_generation_prompt=agp)
    enc = tok.encode(s)
    print(f"add_generation_prompt={agp}")
    print("  characters :", len(s))
    print("  ids        :", len(enc.ids))
    print("  last 5     :", enc.tokens[-5:])
    print("  ends with  :", repr(s[-24:]))
```

**Line by line:**

- `add_generation_prompt: bool = False` defaults to `False`, and the default is the safer one. If somebody
  forgets the argument while building a training set they get correct training data; forgetting it while
  serving produces the obvious, immediately visible failure of a model continuing the conversation on its
  own. Defaults should fail toward the loud direction.
- The appended string is `"<|im_start|>assistant" + "\n"` — written as two pieces on purpose. That trailing
  newline is a design decision, not punctuation, and part
  [5.1](../05-template-mismatch/5.1-one-character-three-tokens-a-different-model.md) is entirely about it.
  Buried inside a longer literal it would be invisible to a reader and to a reviewer.
- The role in the generation prompt is **hardcoded** as `assistant`, not taken from a variable. That is
  correct: you are not rendering a message, you are opening the turn you want the model to fill, and it is
  always the assistant's.
- `enc.tokens[-5:]` looks at the **end** of the sequence, which is the opposite of part
  [4.2](4.2-roles-as-markers.md)'s `[:8]`. The front of a prompt is structure the model has already
  conditioned on; the last few tokens are what it is about to continue from, and they are where prompt bugs
  live.
- `repr(s[-24:])` prints the tail as a literal so the trailing newline is visible as `\n` rather than as an
  empty line at the bottom of the terminal.

Measured on this machine, 2026-08-29, corpus sha256 `9760f2b6d4340b97`:

```text
add_generation_prompt=False
  characters : 170
  ids        : 55
  last 5     : ['ir', 's', '.', '<|im_end|>', 'Ċ']
  ends with  : 'f byte pairs.<|im_end|>\n'
add_generation_prompt=True
  characters : 192
  ids        : 60
  last 5     : ['<|im_start|>', 'ass', 'ist', 'ant', 'Ċ']
  ends with  : '>\n<|im_start|>assistant\n'
```

**Five ids** is the whole cost of the generation prompt: `<|im_start|>`, then `ass`, `ist`, `ant`, then `Ċ`
— the newline.

Look at those middle three. The word `assistant` is **three ordinary tokens**, exactly as part
[4.2](4.2-roles-as-markers.md) said roles would be. There is nothing special about them; `ist` appears in
`list`, `exist` and `consist` throughout ordinary English. The only thing that makes this sequence mean
*your turn* is that it follows `<|im_start|>` and that the model has seen the pattern thousands of times.

And the last token is `Ċ`, the newline. That single id is the immediate left-context of the model's first
generated token. Everything about the answer's opening — whether it starts with a capital, whether it starts
with a space — is conditioned on that one id being what training used.

```mermaid
sequenceDiagram
    participant A as application
    participant T as template
    participant M as model
    A->>T: messages + add_generation_prompt=True
    T->>M: 60 ids, ending &lt;|im_start|&gt; ass ist ant Ċ
    Note over M: the turn is open and empty
    M-->>M: sample id, append, repeat
    M->>A: ... until it emits &lt;|im_end|&gt;
    Note over A: the serving loop halts on that id (part 1.1)
```

---

## Shapes

| Tensor | Symbolic shape | Concrete here | What each axis means |
| --- | --- | --- | --- |
| training example ids | `(T,)` | `(55,)` | every turn opened and closed |
| inference prompt ids | `(T,)` | `(60,)` | plus the 5-token generation prompt |
| the generation prompt itself | `(5,)` | `(5,)` | `<\|im_start\|>` · `ass` · `ist` · `ant` · newline |
| logits at the last position | `(V,)` | `(1004,)` | the distribution the newline token conditions |
| the growing sequence | `(T + n,)` | `(60 + n,)` | one id appended per sampling step, until `<\|im_end\|>` |

The row that matters at serving time is the last one. Every sampling step re-runs the model on a sequence
one token longer, which is why Day 73's KV cache exists — but the *prompt* portion, all 60 ids of it, is
fixed for the whole generation. Structural overhead is paid once per request, not once per token, and that
is why five tokens is a price worth paying for an unambiguous boundary.

---

## When it breaks

**The loud version.** There is not one. Both strings are valid, both tokenize, both round-trip. This is a
correctness bug with no exception attached, in either direction.

**The silent version, direction one: served without a generation prompt.** The model is handed a completed
conversation and asked to continue it. Verbatim, the shape of what comes back:

```text
prompt (as rendered):
<|im_start|>user
What is a merge list?<|im_end|>

model output:
<|im_start|>assistant
An ordered table of byte pairs.<|im_end|>
<|im_start|>user
And how is it built?<|im_end|>
<|im_start|>assistant
By counting adjacent pairs and merging the commonest, repeatedly.<|im_end|>
```

The model answered — and then invented the user's next question and answered that too, because that is what
the training data does after `<|im_end|>\n`. Your serving loop halts at the first `<|im_end|>`, so you may
never see the rest; what you see is a correct answer, and everything looks fine. Until a request where the
model's *first* act is to open a user turn, and the reply is a question the user never asked.

**The silent version, direction two: trained with a generation prompt.** Every training example ends with a
dangling `<|im_start|>assistant\n` after the assistant's closed turn. The model learns that a completed
assistant turn is followed by *another* opened assistant turn, and at inference it produces two replies back
to back. Loss falls normally throughout training; nothing in the metrics distinguishes this corpus from a
correct one.

**The check that catches both,** and it can go red:

```python
train = render_by_hand(chat, add_generation_prompt=False)
serve = render_by_hand(chat[:-1], add_generation_prompt=True)

assert train.endswith("<|im_end|>\n"), f"training example ends {train[-20:]!r}, not with a closed turn"
assert serve.endswith("<|im_start|>assistant\n"), f"prompt ends {serve[-24:]!r}, not with an open turn"
assert serve.count("<|im_start|>") == serve.count("<|im_end|>") + 1, (
    "the prompt does not have exactly one unclosed turn"
)
assert train.count("<|im_start|>") == train.count("<|im_end|>"), (
    "a training example has an unclosed turn - it carries a generation prompt"
)
print(f"train {len(tok.encode(train).ids)} ids, serve {len(tok.encode(serve).ids)} ids")
```

**Line by line:**

- `chat[:-1]` drops the assistant's message before rendering the serving prompt. That is what a real request
  looks like — the conversation ends with the user — and rendering the full list would have produced a
  prompt asking the model to answer after it had already answered.
- The two `endswith` assertions pin the **tail**, which is the only part of a prompt that differs between
  the two modes. Comparing whole strings would fail for a hundred irrelevant reasons; comparing tails fails
  only for this one.
- The marker-counting assertions are the structural version of the same claim, and they are worth having as
  well as the string checks because they keep working if the marker text changes. One unclosed turn in the
  prompt, zero in the training example — stated as arithmetic on counts.
- The failure messages quote the actual tail with `repr`. A trailing newline is the thing most likely to be
  wrong and the thing least likely to be visible.

---

## In production

**Where the flag lives.** In the request path, set once, next to the code that builds the message list —
never defaulted from a config file. It is not a tuning knob; it is a statement about which of two jobs you
are doing, and the two jobs live in different files.

**What a research engineer checks when a fine-tune produces double replies.** This, before anything about
the model: render one training example, print `repr` of its last 30 characters, and look for a dangling
`<|im_start|>assistant\n`. It is thirty seconds and it explains the symptom completely when it is the cause.

**The cost, at conversation scale.** Five tokens per request, once, for the generation prompt. The per-turn
cost is larger, and you can measure it directly by rendering the same conversation with every `content` set
to the empty string — what is left is exactly the format. Measured here, on the three-turn conversation:

```text
full     : 55
structure: 20 (36.4% of the rendered conversation)
content  : 35
structure tokens: ['<|im_start|>', 's', 'y', 'stem', 'Ċ', '<|im_end|>', 'Ċ',
                   '<|im_start|>', 'us', 'er', 'Ċ', '<|im_end|>', 'Ċ',
                   '<|im_start|>', 'ass', 'ist', 'ant', 'Ċ', '<|im_end|>', 'Ċ']
```

**36.4% of that conversation is format.** Six marker tokens, eight tokens of role names, six newlines. The
fraction falls as turns get longer and rises as they get shorter, so a chat application handling many brief
exchanges spends a startling share of its context window on punctuation. The measurement is four lines and
is worth running on your own traffic before choosing a template.

**The failure that only shows with real data.** A conversation ending with an *assistant* message — because
the user is asking for a continuation, or because a retry re-sent the history. Rendering that with a
generation prompt produces two consecutive assistant turns, the second one empty and open. The model has
probably never seen that in training, and its behaviour is undefined rather than merely wrong. Templates in
production usually guard against it; the guard is a conditional inside the template, which is exactly what
part [4.5](4.5-a-published-template-read-line-by-line.md) finds in a real one.

**The review comment:** *"Print `repr` of the last 30 characters of one rendered training example and one
rendered prompt, side by side, in the PR description."* It is the cheapest possible evidence and it makes
this class of bug impossible to merge.

**The interview question:** *"What is `add_generation_prompt` and what happens if you get it wrong?"* The
answer that shows experience gives **both** directions — the model continues the conversation on its own if
you omit it at serving, and it learns to emit a second opened turn if you include it in training — and then
observes that neither one raises. Naming only the first direction is the answer of somebody who has served a
model but never built a training set.

---

## Check yourself

Run this — it prints two id counts and two tails, not a pass:

```text
uv run python -c "
from tokenizers import Tokenizer, models, pre_tokenizers, decoders, trainers
import pathlib
t = Tokenizer(models.BPE()); t.pre_tokenizer = pre_tokenizers.ByteLevel(add_prefix_space=False)
t.decoder = decoders.ByteLevel()
t.train_from_iterator([pathlib.Path('docs/00_MASTER_PLAN.md').read_text(encoding='utf-8')],
  trainer=trainers.BpeTrainer(vocab_size=1000, show_progress=False,
                              initial_alphabet=pre_tokenizers.ByteLevel.alphabet()))
t.add_special_tokens(['<|pad|>','<|im_start|>','<|im_end|>'])
chat = [
  {'role': 'system',    'content': 'You answer in one sentence.'},
  {'role': 'user',      'content': 'What is a merge list?'},
  {'role': 'assistant', 'content': 'An ordered table of byte pairs.'},
]
NL = chr(10)
def r(ms, agp=False):
    o = [''.join(['<|im_start|>', m['role'], NL, m['content'], '<|im_end|>', NL]) for m in ms]
    if agp: o.append('<|im_start|>assistant' + NL)
    return ''.join(o)
for agp in (False, True):
    s = r(chat, agp); e = t.encode(s)
    print(f'agp={agp}: {len(s)} chars, {len(e.ids)} ids, last5={e.tokens[-5:]}')
"
```

Expected: `170 chars, 55 ids` then `192 chars, 60 ids`, with the second tail being
`['<|im_start|>', 'ass', 'ist', 'ant', 'Ċ']`.

Then answer out loud, without scrolling up:

1. What is a generation prompt, physically? Describe it without using the word "prompt".
2. Five tokens, four of which are ordinary text. Say why that is fine, and what would break if `assistant`
   *were* a special token.
3. Give both failure directions — omitted at serving, included in training — and say what each looks like in
   the output.
4. The last token of the prompt is a newline. Name what depends on it, and the day that generalises this
   problem.
