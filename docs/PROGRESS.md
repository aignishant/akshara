# Progress Ledger — Project Akshara

Append-only. **The last row is where we actually are.** One row per *completed* day, pasted from
that day's hub §11 before `./m done N` will commit. A day with no row here is not finished,
whatever the folder looks like.

`Parts` is the part-document count, not a score — but a day closing three IDs with two parts is
thin, and thin is visible from this column alone (plan §27).

| Day | Date | IDs closed | Parts | Tier | Commit | Gates green? |
| --- | ---- | ---------- | ----- | ---- | ------ | ------------ |
